:80 {
    # API Backend - MUST come before catch-all
    handle /api/* {
        reverse_proxy backend:10000
    }

    # Frontend (Catch-all for SPA)
    handle {
        reverse_proxy frontend:80
    }
}
